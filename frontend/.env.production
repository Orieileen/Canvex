# 生产环境 API 基址（Vercel 构建时 Vite 会自动读取此文件）
# 说明：若在 Vercel 项目设置了同名环境变量，将优先生效；此文件作为兜底，避免构建时未注入导致前端请求落到自身域名而 404/405。
VITE_API_URL=https://mokouin.ngrok.app

# 前端自身站点地址（可选，仅用于前端内部跳转/展示）
VITE_FRONTEND_URL=https://www.meired.com

