# ╔══════════════════════════════════════════════════════════════════╗
# ║  Canvex  —  环境变量参考                                       ║
# ║  按 Graph 节点 → Tool 依赖 → 基础设施 分组                     ║
# ╚══════════════════════════════════════════════════════════════════╝


# =================================================================
# 1. 聊天链路 — LangGraph call_llm 节点 (graphs.py)
#    流程: load_memory → call_llm → update_memory → END
# =================================================================

# [必填] 聊天 LLM 的 API Key
OPENAI_API_KEY=

# 聊天 LLM 的 Base URL。直连 OpenAI 可留空；兼容网关填完整地址（如 https://xxx/v1）
OPENAI_BASE_URL=

# 聊天模型名称
CHAT_MODEL=gpt-4o-mini

# 聊天温度
CHAT_TEMPERATURE=0.4

# 聊天最大 token 数（留空则由模型决定）
# CHAT_MAX_TOKENS=


# =================================================================
# 2. 媒体链路 — 生图 / 改图 / 视频共用客户端 (tools/common.py)
#    若与聊天走同一服务商，可与上方填相同值
# =================================================================

# [必填] 媒体链路 API Key
MEDIA_API_KEY=

# [必填] 媒体链路 Base URL
MEDIA_BASE_URL=

# 媒体链路全局超时（秒）
MEDIA_TIMEOUT=600

# 可选：兼容端点路径。填写后不走 OpenAI SDK 默认端点，而是直接请求
# MEDIA_BASE_URL + 该路径。支持填完整 URL，或仅填相对路径（如 /videos）。
# 各变量详见下方对应小节。


# =================================================================
# 3. imagetool — 生图 (tools/image.py → _generate_image_media)
#    通过 Responses API + image_generation tool 生成图片
# =================================================================

# [必填] Responses API 的 chat model
MEDIA_RESPONSES_MODEL=gpt-5-mini

# 生图使用的图片模型（留空则不启用 imagetool）
MEDIA_IMAGE_MODEL=gpt-image-1.5

# 可选：兼容生图端点；填写后最终请求地址 = MEDIA_BASE_URL + 该路径
# MEDIA_IMAGE_COMPAT_ENDPOINT=/images/generations


# =================================================================
# 4. 图片编辑 — ExcalidrawImageEditJob (tasks.py → _edit_image_media)
#    同样走 Responses API + image_generation(action=edit)
# =================================================================

# [必填] 编辑使用的图片模型
MEDIA_IMAGE_EDIT_MODEL=gpt-image-1.5

# 可选：兼容图片编辑端点；填写后最终请求地址 = MEDIA_BASE_URL + 该路径
# MEDIA_IMAGE_EDIT_COMPAT_ENDPOINT=/images/generations

# 编辑输入图片的保真度：high | low（仅 gpt-image-1.5 系列生效）
MEDIA_IMAGE_EDIT_INPUT_FIDELITY=high

# 编辑失败时是否允许退化为纯文本生图（忽略原图）：0 | 1
# IMAGE_EDIT_ALLOW_TEXT_FALLBACK=0

# 聊天接口等待图片工具结果的最长时间（秒），默认 1200 = 20 分钟
IMAGE_TOOL_WAIT_TIMEOUT_SECONDS=1200


# =================================================================
# 5. videotool — 视频生成 (tools/video.py, tasks.py, video_script.py)
# =================================================================

# 视频生成模型
MEDIA_VIDEO_MODEL=sora-2

# 可选：兼容视频生成端点；填写后最终请求地址 = MEDIA_BASE_URL + 该路径
# MEDIA_VIDEO_COMPAT_ENDPOINT=/videos

# 视频默认时长（秒），允许值: 4, 8, 12
MEDIA_VIDEO_SECONDS_DEFAULT=12

# 视频轮询参数
MEDIA_VIDEO_POLL_INTERVAL=5
MEDIA_VIDEO_POLL_MAX_ATTEMPTS=360
MEDIA_VIDEO_TIMEOUT_SECONDS=1800

# 视频分镜脚本分析模型
MEDIA_SCRIPT_MODEL=gpt-5-mini

# 分镜分析图片参数（一般无需修改）
# MEDIA_SCRIPT_IMAGE_MAX_SIDE=1280
# MEDIA_SCRIPT_IMAGE_JPEG_QUALITY=85

# 视频生成失败重试参数（一般无需修改）
# VIDEO_RETRY_MAX=6
# VIDEO_RETRY_BASE_SECONDS=20
# VIDEO_RETRY_MAX_DELAY_SECONDS=180


# =================================================================
# 6. 抠图 — cutout / rembg (tasks.py)
#    一般无需修改，仅在抠图质量不佳时调整
# =================================================================

# CUTOUT_REMBG_ALPHA_MATTING=1
# CUTOUT_REMBG_FOREGROUND_THRESHOLD=240
# CUTOUT_REMBG_BACKGROUND_THRESHOLD=10
# CUTOUT_REMBG_ERODE_SIZE=6


# =================================================================
# 7. 记忆系统 — load_memory / update_memory 节点 (memory.py)
# =================================================================

# 摘要 TTL（秒），默认 30 天
# SUMMARY_TTL_SECONDS=2592000

# 摘要保留快照数
# SUMMARY_HISTORY_LIMIT=6

# 记忆稳定性窗口 / 最小出现次数
# MEMORY_STABILITY_WINDOW=3
# MEMORY_STABILITY_MIN_COUNT=2


# =================================================================
# 8. Frontend (Vite 构建注入)
# =================================================================

# 后端 API 地址
VITE_API_URL=http://localhost:28000

# Excalidraw 静态资源路径
VITE_CANVEX_ASSET_PATH=/canvex-assets/

# Workspace 标识（前后端需一致）
VITE_WORKSPACE_KEY=public
WORKSPACE_ID=public

# 前端轮询参数 — 图片编辑（maxAttempts × intervalMs = 超时）
VITE_IMAGE_EDIT_POLL_MAX_ATTEMPTS=600
VITE_IMAGE_EDIT_POLL_INTERVAL_MS=3000

# 前端轮询参数 — 视频
VITE_VIDEO_POLL_MAX_ATTEMPTS=600
VITE_VIDEO_POLL_INTERVAL_MS=3000

# 场景保存节流参数（毫秒，一般无需修改）
# VITE_SCENE_SAVE_DEBOUNCE_MS=600
# VITE_SCENE_SAVE_URGENT_MS=180
# VITE_SCENE_SAVE_FORCE_FLUSH_MS=1600
# VITE_SCENE_SAVE_WATCH_INTERVAL_MS=700
# VITE_SCENE_LOCAL_CACHE_DEBOUNCE_MS=400
# VITE_SCENE_SYNC_INTERVAL_MS=5000

# 场景图片最大边长（像素）
# VITE_CANVEX_MAX_SCENE_IMAGE_DIM=1600


# =================================================================
# 9. Django 基础
# =================================================================

DJANGO_SECRET_KEY=change-me-please-if-prd
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend
# DJANGO_TIME_ZONE=UTC

CORS_ALLOW_ALL_ORIGINS=true
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:28000


# =================================================================
# 10. MySQL
# =================================================================

MYSQL_DATABASE=canvex
MYSQL_USER=canvex
MYSQL_PASSWORD=canvex
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=change-me-root-password


# =================================================================
# 11. Redis / Celery
# =================================================================

REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_TASK_ALWAYS_EAGER=false


# =================================================================
# 12. URL / 端口映射 (docker-compose)
# =================================================================

# [必填] 公开媒体地址（_abs_url 用于拼接资源完整路径）
PUBLIC_MEDIA_BASE=http://localhost:28000
INTERNAL_MEDIA_BASE=http://backend:8000

BACKEND_PORT=28000
FRONTEND_PORT=5173

# 上传大小限制（字节），默认 20MB
# DATA_UPLOAD_MAX_MEMORY_SIZE=20971520
# FILE_UPLOAD_MAX_MEMORY_SIZE=20971520
