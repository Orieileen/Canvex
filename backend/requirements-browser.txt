# Optional deps for the agentic browser tool (`browse`), enabled by
# CANVAS_BROWSER_ENABLED=true. Split out of requirements.txt because browser-use
# pulls a full Chromium (heavy) and the feature is off by default.
#
# Install:
#   pip install -r requirements-browser.txt
#   python -m playwright install chromium      # (Docker: add --with-deps)
#
# NOTE: browser-use has a FAST-MOVING API. Pin an exact version you have tested —
# studio/services/agent/browser_runner.py is the single adapter seam that tracks
# it, so a version bump only ever touches that one file. The base requirements.txt
# in this repo is intentionally unpinned; match that style or pin here as you like.
browser-use
playwright
